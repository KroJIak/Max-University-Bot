# University API

API для работы с сайтом университета через web scraping. Предоставляет endpoints для получения данных студентов, расписания, преподавателей и другой информации с официального сайта университета.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Установка и запуск](#установка-и-запуск)
- [API Endpoints](#api-endpoints)
- [Переменные окружения](#переменные-окружения)
- [Структура проекта](#структура-проекта)
- [Разработка](#разработка)

## 🎯 Описание

**University API** — это микросервис на FastAPI, который выполняет web scraping официального сайта университета для получения данных студентов. Сервис автоматически управляет сессиями пользователей, сохраняя cookies в базе данных PostgreSQL.

### Основные возможности:

- 🔐 **Аутентификация студентов**: Логин на сайтах университета с сохранением сессии
- 📅 **Расписание**: Получение расписания занятий за указанный период
- 👨‍🏫 **Преподаватели**: Список преподавателей и информация о них
- 📊 **Личные данные**: Получение структурированных данных студента
- 📞 **Контакты**: Информация о деканатах и кафедрах
- 📰 **Новости**: Получение новостей университета
- 🗺️ **Карты**: Информация о корпусах университета
- 🔗 **Платформы**: Список полезных веб-платформ

## 🚀 Возможности

### Управление сессиями

API автоматически управляет cookies сессии для каждого студента:
- Cookies сохраняются в БД после успешного логина
- Используются для последующих запросов без повторной аутентификации
- Поддержка cookies для разных доменов (`tt.chuvsu.ru`, `lk.chuvsu.ru`)

### Web Scraping

Все данные парсятся с официального сайта университета с помощью:
- `requests` для HTTP-запросов
- `beautifulsoup4` для парсинга HTML
- Автоматическое управление сессиями и cookies

## 🏗️ Архитектура

```
university-app/
├── api/                    # API endpoints
│   └── students.py        # Роутеры для работы со студентами
├── core/                   # Основная логика
│   └── database.py        # Настройка БД (SQLAlchemy)
├── models/                 # Модели данных
│   └── session_cookies.py # Модель для хранения cookies
├── repositories/           # Репозитории для работы с БД
│   └── session_cookies_repository.py
├── services/               # Бизнес-логика
│   ├── university_scraper.py  # Основной scraper
│   ├── auth.py            # Аутентификация
│   ├── schedule.py        # Расписание
│   ├── teachers.py        # Преподаватели
│   ├── personal_data.py   # Личные данные
│   ├── contacts.py        # Контакты
│   └── news.py            # Новости
├── data/                   # Статические данные
│   └── maps.json          # Данные о корпусах
├── main.py                # Точка входа FastAPI
├── requirements.txt       # Зависимости
└── Dockerfile             # Docker конфигурация
```

## 📦 Установка и запуск

### Требования

- Python 3.11+
- PostgreSQL 15+
- Docker (опционально)

### Локальный запуск

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте переменные окружения:**
   ```bash
   export UNIVERSITY_DATABASE_URL="postgresql://user:password@localhost:5432/university_db"
   export UNIVERSITY_API_HOST="0.0.0.0"
   export UNIVERSITY_API_PORT="8001"
   ```

3. **Запустите сервис:**
   ```bash
   uvicorn main:app --host 0.0.0.0 --port 8001
   ```

### Запуск через Docker

1. **Соберите образ:**
   ```bash
   docker build -t university-api .
   ```

2. **Запустите контейнер:**
   ```bash
   docker run -d \
     -p 8001:8001 \
     -e UNIVERSITY_DATABASE_URL="postgresql://user:password@postgres:5432/university_db" \
     university-api
   ```

### Запуск через Docker Compose

Сервис автоматически запускается при использовании основного `docker-compose.yml` проекта:

```bash
docker compose up -d
```

## 📡 API Endpoints

### Базовые endpoints

- `GET /` - Информация о сервисе
- `GET /health` - Health check
- `GET /docs` - Swagger документация
- `GET /redoc` - ReDoc документация

### Endpoints для студентов

Все endpoints находятся под префиксом `/students`:

#### `POST /students/login`

Выполняет логин на сайтах университета и сохраняет cookies сессии в БД.

**Запрос:**
```json
{
  "student_email": "student@university.ru",
  "password": "password123"
}
```

**Ответ:**
```json
{
  "success": true,
  "error": null
}
```

#### `POST /students/personal_data`

Получает структурированные данные студента (ФИО, группа, курс, фото).

**Запрос:**
```json
{
  "student_email": "student@university.ru"
}
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "full_name": "Иванов Иван Иванович",
    "group": "ИВТ-21-01",
    "course": "3",
    "photo": "data:image/jpeg;base64,..."
  },
  "error": null
}
```

#### `POST /students/schedule`

Получает расписание занятий студента за указанный период.

**Запрос:**
```json
{
  "student_email": "student@university.ru",
  "date_range": "10.11-03.12"
}
```

**Ответ:**
```json
{
  "success": true,
  "schedule": [
    {
      "date": "2024-01-15",
      "time_start": "09:00",
      "time_end": "10:30",
      "subject": "Математика",
      "type": "Лекция",
      "teacher": "Иванов И.И.",
      "room": "101"
    }
  ],
  "error": null
}
```

#### `POST /students/teachers`

Получает список всех преподавателей университета.

**Запрос:**
```json
{
  "student_email": "student@university.ru"
}
```

**Ответ:**
```json
{
  "success": true,
  "teachers": [
    {"id": "tech0001", "name": "Иванов Иван Иванович"},
    {"id": "tech0002", "name": "Петров Петр Петрович"}
  ],
  "error": null
}
```

#### `POST /students/teacher_info`

Получает информацию о конкретном преподавателе (кафедры, фото).

**Запрос:**
```json
{
  "student_email": "student@university.ru",
  "teacher_id": "2173"
}
```

**Ответ:**
```json
{
  "success": true,
  "departments": ["Кафедра информатики"],
  "photo": "data:image/jpeg;base64,...",
  "error": null
}
```

#### `POST /students/contacts`

Получает контактную информацию деканатов и кафедр.

**Запрос:**
```json
{
  "student_email": "student@university.ru"
}
```

**Ответ:**
```json
{
  "success": true,
  "deans": [
    {
      "faculty": "Факультет информатики",
      "phone": "+7 (123) 456-78-90",
      "email": "dean@university.ru"
    }
  ],
  "departments": [
    {
      "faculty": "Факультет информатики",
      "department": "Кафедра программирования",
      "phones": "+7 (123) 456-78-90",
      "email": "dept@university.ru"
    }
  ],
  "error": null
}
```

#### `GET /students/platforms`

Получает список полезных веб-платформ университета. Не требует аутентификации.

**Ответ:**
```json
{
  "success": true,
  "platforms": [
    {
      "key": "requests",
      "name": "Запросы и справки",
      "url": "https://lk.chuvsu.ru/student/request.php",
      "emoji": "📋"
    }
  ],
  "error": null
}
```

#### `GET /students/services`

Получает список сервисов университета. Не требует аутентификации.

**Ответ:**
```json
{
  "success": true,
  "services": [
    {"key": "schedule", "name": "Расписание", "emoji": "📅"},
    {"key": "teachers", "name": "Преподаватели", "emoji": "👨‍🏫"}
  ],
  "error": null
}
```

#### `GET /students/maps`

Получает список корпусов университета с координатами и ссылками на карты. Не требует аутентификации.

**Ответ:**
```json
{
  "buildings": [
    {
      "name": "Главный корпус",
      "latitude": 56.123456,
      "longitude": 47.123456,
      "yandex_map_url": "https://yandex.ru/maps/?pt=47.123456,56.123456&z=17",
      "gis2_map_url": "https://2gis.ru/cheboksary/firm/70000001012345678",
      "google_map_url": "https://www.google.com/maps?q=56.123456,47.123456"
    }
  ]
}
```

#### `GET /students/news`

Получает список новостей университета. Не требует аутентификации.

**Параметры запроса:**
- `limit` (опционально): Максимальное количество новостей (по умолчанию 10)

**Ответ:**
```json
{
  "success": true,
  "news": [
    {
      "id": "news_1",
      "title": "Открытие нового учебного корпуса",
      "content": "Университет рад сообщить...",
      "date": "15.12.2024",
      "author": "Администрация университета",
      "category": "Общие новости",
      "image_url": null,
      "link": "https://www.chuvsu.ru/news/1"
    }
  ],
  "error": null
}
```

## 🔧 Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `UNIVERSITY_DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://maxbot:maxbot123@postgres:5432/university_db` |
| `UNIVERSITY_API_HOST` | Хост для запуска API | `0.0.0.0` |
| `UNIVERSITY_API_PORT` | Порт для запуска API | `8001` |

## 📁 Структура проекта

### `api/students.py`

Содержит все API endpoints для работы со студентами. Использует Pydantic модели для валидации запросов и ответов.

### `core/database.py`

Настройка подключения к PostgreSQL через SQLAlchemy. Автоматически создает БД при первом запуске, если её не существует.

### `models/session_cookies.py`

Модель SQLAlchemy для хранения cookies сессии студентов в БД.

### `repositories/session_cookies_repository.py`

Репозиторий для работы с cookies: создание, обновление, получение по email.

### `services/university_scraper.py`

Основной класс для web scraping. Содержит методы для:
- Логина на сайтах университета
- Получения данных со страниц
- Парсинга HTML

### `services/`

Модули с бизнес-логикой для различных типов данных:
- `auth.py` - Аутентификация
- `schedule.py` - Расписание
- `teachers.py` - Преподаватели
- `personal_data.py` - Личные данные
- `contacts.py` - Контакты
- `news.py` - Новости

## 🛠️ Разработка

### Зависимости

Основные зависимости указаны в `requirements.txt`:
- `fastapi` - Web framework
- `uvicorn` - ASGI server
- `sqlalchemy` - ORM для работы с БД
- `psycopg2-binary` - PostgreSQL драйвер
- `requests` - HTTP клиент
- `beautifulsoup4` - HTML парсер
- `pydantic` - Валидация данных

### Тестирование

Для тестирования API используйте Swagger UI:
```
http://localhost:8001/docs
```

Или ReDoc:
```
http://localhost:8001/redoc
```

### Логирование

API использует стандартное логирование Python. Логи выводятся в консоль.

### Обработка ошибок

API возвращает стандартные HTTP статус коды:
- `200` - Успешный запрос
- `401` - Неавторизован (сессия истекла или неверные credentials)
- `404` - Cookies не найдены (необходимо выполнить логин)
- `500` - Внутренняя ошибка сервера

### Особенности

1. **Тестовый аккаунт**: Для `test@test.ru/test` используется реальный аккаунт `goliluxa@mail.ru/P17133p17133`
2. **Автоматическое создание БД**: При первом запуске API автоматически создает БД, если её не существует
3. **Управление cookies**: Cookies сохраняются отдельно для каждого домена (`tt.chuvsu.ru`, `lk.chuvsu.ru`)

## 🔗 Интеграция

University API используется другими сервисами проекта:
- **MAX API** (`app/`) - Основной API, который вызывает University API для получения данных студентов
- **Bot** (`bot/`) - Бот может использовать University API через MAX API

## 📝 Примечания

- Все данные парсятся с официального сайта университета
- Cookies сессии хранятся в БД и автоматически используются для последующих запросов
- При истечении сессии необходимо повторно выполнить логин
- API поддерживает CORS для работы с веб-приложениями

## 📄 Лицензия

MIT

